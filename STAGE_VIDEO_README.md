# 舞台欣赏栏目 - 使用说明

## 📁 需要添加的文件

### 1. 视频文件
- **路径**: `videos/stage-performance.mp4`
- **格式**: MP4 (推荐 H.264 编码)
- **建议分辨率**: 1920x1080 (1080p) 或 1280x720 (720p)
- **建议时长**: 3-5分钟

**如何准备视频：**
1. 在项目根目录创建 `videos` 文件夹（如果不存在）
2. 将你的舞台表演视频命名为 `stage-performance.mp4`
3. 放入 `videos` 文件夹中

### 2. 视频封面图（可选但推荐）
- **路径**: `images/stage-poster.jpg`
- **格式**: JPG 或 PNG
- **建议尺寸**: 1920x1080 像素
- **用途**: 视频加载前显示的缩略图

**如何准备封面：**
1. 从视频中截取一帧作为封面
2. 或使用舞台表演的精美照片
3. 命名为 `stage-poster.jpg`
4. 放入 `images` 文件夹中

##  功能说明

### 播放器特性
✅ **原生 HTML5 视频播放器**
- 点击播放按钮即可播放
- 支持暂停、快进、后退
- 音量控制
- 全屏播放
- 进度条拖动

✅ **自动适配**
- 桌面端：最大宽度 1000px，居中显示
- 移动端：自适应屏幕宽度
- 保持视频宽高比

✅ **视觉效果**
- 圆角边框（20px）
- 阴影效果
- 渐变背景
- 黑色视频容器

## 🔧 自定义修改

### 更换视频
在 `index.html` 第 113 行修改：
```html
<source src="videos/你的视频名.mp4" type="video/mp4">
```

### 更换封面
在 `index.html` 第 112 行修改：
```html
poster="images/你的封面图.jpg"
```

### 调整样式
在 `style.css` 中搜索 `.stage-section` 相关样式进行修改：
- 背景颜色
- 内边距
- 视频容器大小
- 圆角半径等

## 📱 浏览器兼容性

✅ Chrome / Edge
✅ Firefox
✅ Safari
✅ 移动端浏览器

所有现代浏览器都支持 HTML5 `<video>` 标签。

##  提示

1. **视频优化**：建议使用在线工具压缩视频，减小文件大小
2. **多格式支持**：如需更好兼容性，可添加 WebM 格式备用
3. **自动播放**：如需自动播放，添加 `autoplay muted` 属性
4. **循环播放**：如需循环，添加 `loop` 属性

---

**当前状态**：栏目已创建，等待你添加视频文件后即可使用！
